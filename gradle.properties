# Project version — single source of truth, consumed by all modules and bumped
# by scripts/bump-version.sh (see __docs/BRANCHING.md). Keep MAJOR.MINOR.PATCH.
version=0.1.0

# Gradle
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true

# Kotlin
kotlin.code.style=official

# Android (used once the Android target is added in Phase 0)
android.useAndroidX=true
android.nonTransitiveRClass=true
